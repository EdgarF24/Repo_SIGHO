===============================================================================
🏨 SIGHO - SISTEMA INTEGRADO DE GESTIÓN HOTELERA
===============================================================================
        INSTRUCCIONES COMPLETAS PARA PONER A CORRER EL PROYECTO
===============================================================================

📋 ÍNDICE
---------
1. Requisitos Previos
2. Estructura del Proyecto
3. Instalación Automática (Recomendado)
4. Instalación Manual
5. Ejecutar el Sistema
6. Credenciales de Acceso
7. Verificación del Sistema
8. Solución de Problemas Comunes
9. Información Adicional

===============================================================================
1. REQUISITOS PREVIOS
===============================================================================

Antes de comenzar, asegúrate de tener instalado:

✅ Python 3.12 o superior
   - Verificar versión: python3 --version
   - Si no está instalado:
     sudo apt update
     sudo apt install python3 python3-pip python3-venv python3-tk

✅ pip (gestor de paquetes de Python)
   - Verificar: pip3 --version

✅ curl (opcional, para verificar el backend)
   - Verificar: curl --version
   - Instalar: sudo apt install curl

===============================================================================
2. ESTRUCTURA DEL PROYECTO
===============================================================================

El proyecto SIGHO tiene la siguiente estructura:

SIGHO/
├── sigho-system/
│   ├── backend/              # API REST con FastAPI
│   │   ├── app/              # Código de la aplicación
│   │   ├── main.py           # Punto de entrada del backend
│   │   ├── requirements.txt  # Dependencias del backend
│   │   └── venv/             # Entorno virtual (se crea al instalar)
│   │
│   ├── frontend/             # Interfaz gráfica con CustomTkinter
│   │   ├── app/              # Código de la aplicación
│   │   ├── main.py           # Punto de entrada del frontend
│   │   ├── requirements.txt  # Dependencias del frontend
│   │   └── venv/             # Entorno virtual (se crea al instalar)
│   │
│   ├── install_sigho.sh      # Script de instalación automática
│   ├── start_sigho.sh        # Script para iniciar el sistema
│   └── README.md             # Documentación del proyecto

===============================================================================
3. INSTALACIÓN AUTOMÁTICA (RECOMENDADO) ⭐
===============================================================================

PASO 1: Navegar al directorio del proyecto
-------------------------------------------
cd /home/edgar24/Documentos/SIGHO/sigho-system


PASO 2: Dar permisos de ejecución al script de instalación
-----------------------------------------------------------
chmod +x install_sigho.sh


PASO 3: Ejecutar el script de instalación
------------------------------------------
./install_sigho.sh

Este script hará automáticamente:
- Verificar que Python 3 esté instalado
- Crear entornos virtuales para backend y frontend
- Instalar todas las dependencias necesarias
- Crear archivo de configuración .env para el backend
- Configurar todo el sistema

⏱️  Tiempo estimado: 3-5 minutos

===============================================================================
4. INSTALACIÓN MANUAL (Alternativa)
===============================================================================

Si prefieres instalar manualmente o si el script automático falla:

PASO 1: Navegar al directorio del proyecto
-------------------------------------------
cd /home/edgar24/Documentos/SIGHO/sigho-system


PASO 2: Instalar el Backend
----------------------------

2.1. Entrar al directorio del backend:
      cd backend

2.2. Crear el entorno virtual:
      python3 -m venv venv

2.3. Activar el entorno virtual:
      source venv/bin/activate

2.4. Actualizar pip:
      pip install --upgrade pip

2.5. Instalar dependencias:
      pip install -r requirements.txt

2.6. Crear archivo .env (copiar configuración del README.md o usar el ejemplo):
      nano .env
      
      Contenido mínimo del .env:
      ---------------------------
      APP_NAME=SIGHO - Sistema Integrado de Gestión Hotelera
      APP_VERSION=1.0.0
      DEBUG=True
      HOST=127.0.0.1
      PORT=8000
      DATABASE_URL=sqlite:///./sigho.db
      SECRET_KEY=tu_clave_secreta_super_segura_cambiala_en_produccion_12345
      ALGORITHM=HS256
      ACCESS_TOKEN_EXPIRE_MINUTES=30
      ALLOWED_ORIGINS=http://localhost:*,http://127.0.0.1:*
      HOTEL_NAME=Hotel SIGHO
      HOTEL_ADDRESS=Nueva Esparta, Venezuela
      HOTEL_PHONE=+58 424 1234567
      HOTEL_EMAIL=info@hotelsigho.com
      CURRENCIES=VES,USD,EUR
      TIMEZONE=America/Caracas

2.7. Desactivar el entorno virtual:
      deactivate

2.8. Volver al directorio principal:
      cd ..


PASO 3: Instalar el Frontend
-----------------------------

3.1. Entrar al directorio del frontend:
      cd frontend

3.2. Crear el entorno virtual con acceso a paquetes del sistema:
      python3 -m venv venv --system-site-packages

3.3. Activar el entorno virtual:
      source venv/bin/activate

3.4. Actualizar pip:
      pip install --upgrade pip

3.5. Instalar dependencias:
      pip install -r requirements.txt

3.6. Desactivar el entorno virtual:
      deactivate

3.7. Volver al directorio principal:
      cd ..

===============================================================================
5. EJECUTAR EL SISTEMA
===============================================================================

Tienes dos opciones para ejecutar el sistema:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OPCIÓN A: Ejecución Automática (Un solo comando) ⭐
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PASO 1: Dar permisos de ejecución:
        chmod +x start_sigho.sh

PASO 2: Ejecutar el script:
        ./start_sigho.sh

Este script iniciará automáticamente tanto el backend como el frontend.

PASO 3: Para detener todos los servicios:
        Presiona Ctrl+C en la terminal

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OPCIÓN B: Ejecución Manual (Dos terminales separadas)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔴 TERMINAL 1 - BACKEND:
------------------------

1. Abrir una terminal y navegar al backend:
   cd /home/edgar24/Documentos/SIGHO/sigho-system/backend

2. Activar el entorno virtual:
   source venv/bin/activate

3. Iniciar el backend:
   python main.py

4. Verificar que el backend esté corriendo:
   - Deberías ver un mensaje indicando que Uvicorn está corriendo
   - El backend estará disponible en: http://127.0.0.1:8000
   - Documentación API: http://127.0.0.1:8000/docs


🟢 TERMINAL 2 - FRONTEND:
-------------------------

1. Abrir OTRA terminal (dejar la del backend abierta) y navegar al frontend:
   cd /home/edgar24/Documentos/SIGHO/sigho-system/frontend

2. Activar el entorno virtual:
   source venv/bin/activate

3. Iniciar el frontend:
   python main.py

4. Se abrirá la interfaz gráfica del sistema SIGHO


⚠️  IMPORTANTE: Primero debe estar corriendo el backend ANTES de iniciar 
    el frontend, ya que el frontend necesita conectarse a la API.

===============================================================================
6. CREDENCIALES DE ACCESO
===============================================================================

El sistema crea automáticamente usuarios de prueba. Puedes iniciar sesión
con cualquiera de estos:

┌─────────────────┬──────────────────┬─────────────────┬─────────────────────┐
│ USUARIO         │ CONTRASEÑA       │ ROL             │ PERMISOS            │
├─────────────────┼──────────────────┼─────────────────┼─────────────────────┤
│ admin           │ admin123         │ Administrador   │ Acceso total        │
│ gerente         │ gerente123       │ Gerente         │ Gestión completa    │
│ recepcion       │ recepcion123     │ Recepcionista   │ Reservas y pagos    │
│ mantenimiento   │ manten123        │ Mantenimiento   │ Gestión mantenimien │
│ inventario      │ inventario123    │ Inventario      │ Gestión inventario  │
└─────────────────┴──────────────────┴─────────────────┴─────────────────────┘

🔐 RECOMENDACIÓN: Para acceso completo usa:
    Usuario: admin
    Contraseña: admin123

===============================================================================
7. VERIFICACIÓN DEL SISTEMA
===============================================================================

✅ Verificar que el Backend está funcionando:
---------------------------------------------

Opción 1: Abrir en el navegador web
   http://127.0.0.1:8000/docs
   Deberías ver la documentación interactiva de la API (Swagger UI)

Opción 2: Usar curl desde la terminal
   curl http://127.0.0.1:8000/health
   Debería responder con un mensaje de estado


✅ Verificar que el Frontend está funcionando:
----------------------------------------------

Al ejecutar "python main.py" en el frontend, debería:
1. Abrirse una ventana gráfica con la interfaz del sistema SIGHO
2. Mostrar la pantalla de login
3. Permitir iniciar sesión con las credenciales proporcionadas


✅ Estructura de archivos generados:
------------------------------------

Después de la primera ejecución, se crearán automáticamente:

backend/
└── sigho.db          # Base de datos SQLite con datos iniciales

===============================================================================
8. SOLUCIÓN DE PROBLEMAS COMUNES
===============================================================================

❌ PROBLEMA: "Python 3 no está instalado"
✅ SOLUCIÓN:
   sudo apt update
   sudo apt install python3 python3-pip python3-venv python3-tk


❌ PROBLEMA: "ModuleNotFoundError" al iniciar backend o frontend
✅ SOLUCIÓN:
   1. Asegúrate de activar el entorno virtual:
      source venv/bin/activate
   2. Reinstala las dependencias:
      pip install -r requirements.txt


❌ PROBLEMA: "Connection refused" en el frontend
✅ SOLUCIÓN:
   1. Verifica que el backend esté corriendo primero
   2. Comprueba que el backend esté en http://127.0.0.1:8000
   3. Verifica que no haya firewall bloqueando el puerto 8000


❌ PROBLEMA: Error "tkinter not found"
✅ SOLUCIÓN:
   sudo apt install python3-tk
   Luego recrea el entorno virtual del frontend con --system-site-packages


❌ PROBLEMA: El puerto 8000 ya está en uso
✅ SOLUCIÓN:
   1. Ver qué proceso usa el puerto:
      sudo lsof -i :8000
   2. Matar el proceso:
      kill -9 [PID]
   3. O cambiar el puerto en backend/.env (HOST y PORT)


❌ PROBLEMA: "Permission denied" al ejecutar scripts .sh
✅ SOLUCIÓN:
   chmod +x install_sigho.sh
   chmod +x start_sigho.sh


❌ PROBLEMA: Base de datos corrupta o errores de SQLite
✅ SOLUCIÓN:
   1. Detener el backend
   2. Eliminar la base de datos:
      rm backend/sigho.db
   3. Reiniciar el backend (se creará una nueva base de datos)

===============================================================================
9. INFORMACIÓN ADICIONAL
===============================================================================

📂 Archivos importantes:
-----------------------
• README.md              - Documentación completa del proyecto
• backend/.env           - Configuración del backend
• backend/sigho.db       - Base de datos SQLite
• backend.log            - Log del backend (si usas start_sigho.sh)
• frontend.log           - Log del frontend (si usas start_sigho.sh)


📊 Módulos del Sistema:
-----------------------
• Dashboard              - Estadísticas y métricas en tiempo real
• Habitaciones           - Gestión de habitaciones y disponibilidad
• Reservas               - Check-in, check-out, cancelaciones
• Huéspedes              - Registro y historial de huéspedes
• Pagos                  - Gestión de pagos multi-moneda
• Mantenimiento          - Solicitudes preventivas y correctivas
• Inventario             - Control de stock y movimientos
• Reportes               - Generación de reportes del sistema
• Usuarios               - Gestión de usuarios y permisos
• Configuración          - Ajustes del sistema


🔧 Tecnologías utilizadas:
--------------------------
Backend:
  • FastAPI              - Framework web moderno
  • SQLAlchemy           - ORM para base de datos
  • SQLite3              - Base de datos embebida
  • Uvicorn              - Servidor ASGI
  • Pydantic             - Validación de datos
  • JWT                  - Autenticación

Frontend:
  • CustomTkinter        - Framework GUI moderno
  • Requests             - Cliente HTTP
  • Pillow               - Procesamiento de imágenes
  • Matplotlib           - Gráficos
  • Pandas               - Análisis de datos


📚 Documentación API:
--------------------
Una vez que el backend esté corriendo, accede a:
http://127.0.0.1:8000/docs

Aquí encontrarás documentación interactiva de todos los endpoints disponibles,
podrás probar las APIs directamente desde el navegador.


🌐 URLs importantes:
--------------------
• Backend API:         http://127.0.0.1:8000
• Documentación API:   http://127.0.0.1:8000/docs
• Health Check:        http://127.0.0.1:8000/health


👨‍💻 Equipo de Desarrollo:
--------------------------
• Edgar Fermenio       - Backend
• Andrés Sosa          - Frontend
• Lino Gouveia         - Base de Datos
• Santiago Mendez      - Tester
• Santiago Martin      - Tester


📞 Soporte:
-----------
Email: info@hotelsigho.com
Teléfono: +58 424 1234567


===============================================================================
🎉 ¡LISTO! PASOS RÁPIDOS PARA COMENZAR
===============================================================================

RESUMEN EJECUTIVO:

1. Instalar (solo la primera vez):
   cd /home/edgar24/Documentos/SIGHO/sigho-system
   chmod +x install_sigho.sh
   ./install_sigho.sh

2. Ejecutar el sistema:
   chmod +x start_sigho.sh
   ./start_sigho.sh

3. Iniciar sesión con:
   Usuario: admin
   Contraseña: admin123

4. Para detener:
   Presiona Ctrl+C

¡Disfruta usando SIGHO! 🏨

===============================================================================
Última actualización: 27 de Noviembre de 2024
===============================================================================
